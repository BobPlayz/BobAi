import { parseToolResult } from "./structuredResult.js";
import { boundedExecutionResult, boundedString } from "./executionGuard.js";
const DEFAULT_URL = "http://127.0.0.1:3456"; const MAX_TASK_LENGTH = 20_000; const MAX_RESULT_ITEMS = 200; const MAX_RESULT_BYTES = 2 * 1024 * 1024; const MAX_DIFF_BYTES = 1_000_000; const POLL_MS = 1_000; const TIMEOUT_MS = 5 * 60 * 1_000; const REQUEST_TIMEOUT_MS = 15_000;
type CodingAgentJob = { id: string; status: "queued" | "running" | "completed" | "failed" | "cancelled"; result?: { plan?: unknown; executionResults?: string[]; review?: string; diff?: string; sandboxVerified?: boolean; artifacts?: unknown }; error?: string };
export function isCodingTask(text: string) { return /\b(write|build|create|fix|debug|refactor|implement|code|coding|program|function|component|api|endpoint|typescript|javascript|python|react|next\.js|css|html)\b/i.test(text); }
function getConfig() { const url = (process.env.BOBAI_CODING_AGENT_URL || DEFAULT_URL).replace(/\/$/, ""); const key = process.env.BOBAI_CODING_AGENT_KEY?.trim(); if (!key || key.length < 32) throw new Error("coding agent bridge is not configured"); if (process.env.NODE_ENV === "production" && process.env.BOBAI_CODING_AGENT_SANDBOX_ATTESTED !== "true") throw new Error("coding agent sandbox is not production-attested"); const parsed = new URL(url); if (!["http:", "https:"].includes(parsed.protocol)) throw new Error("coding agent bridge must use HTTP(S)"); if (!["localhost", "127.0.0.1", "::1", "[::1]"].includes(parsed.hostname.toLowerCase())) throw new Error("coding agent bridge must run on localhost"); return { url: parsed.toString().replace(/\/$/, ""), key }; }
async function request<T>(url: string, key: string, init?: RequestInit): Promise<T> { const controller = new AbortController(); const timer = setTimeout(() => controller.abort(), REQUEST_TIMEOUT_MS); try { const response = await fetch(url, { ...init, headers: { "Content-Type": "application/json", Authorization: `Bearer ${key}`, ...init?.headers }, signal: controller.signal, redirect: "error" }); const body: unknown = await response.json().catch(() => ({})); if (!response.ok) { const message = typeof body === "object" && body !== null && "error" in body && typeof body.error === "string" ? body.error.slice(0, 4_000) : `coding agent returned ${response.status}`; throw new Error(message); } return body as T; } finally { clearTimeout(timer); } }
async function requestCancel(url: string, key: string, id: string) { try { await request(`${url}/task/${encodeURIComponent(id)}/cancel`, key, { method: "POST" }); } catch {} }
function normalizeCompletedResult(result: unknown) { if (!result || typeof result !== "object" || Array.isArray(result)) throw new Error("coding agent returned an invalid result"); const value = result as Record<string, unknown>; if (value.plan !== undefined && typeof value.plan !== "string" && (typeof value.plan !== "object" || value.plan === null)) throw new Error("coding agent returned an invalid plan"); if (!Array.isArray(value.executionResults) || value.executionResults.length === 0 || value.executionResults.length > MAX_RESULT_ITEMS || value.executionResults.some((item) => typeof item !== "string" || item.length > 20_000)) throw new Error("coding agent completed without verified execution actions"); if (value.review !== undefined) boundedString(value.review as string, 20_000); if (value.diff !== undefined && (typeof value.diff !== "string" || value.diff.length > MAX_DIFF_BYTES || (!value.diff.startsWith("diff --git ") && !value.diff.includes("@@")))) throw new Error("coding agent returned an invalid diff"); if (process.env.NODE_ENV === "production" && value.sandboxVerified !== true) throw new Error("coding agent result is not sandbox-verified"); if (value.artifacts !== undefined) boundedExecutionResult(value.artifacts); boundedExecutionResult(value); return value as { plan?: unknown; executionResults: string[]; review?: string; diff?: string; sandboxVerified?: boolean; artifacts?: unknown }; }
export async function runCodingAgent(task: string, options: { budget?: unknown; plan?: unknown; checkpointToken?: string } = {}) { const normalizedTask = task.trim(); if (!normalizedTask) throw new Error("coding task is empty"); if (normalizedTask.length > MAX_TASK_LENGTH) throw new Error(`coding task cannot exceed ${MAX_TASK_LENGTH} characters`); const { url, key } = getConfig(); const created = await request<{ id: string; status: CodingAgentJob["status"] }>(`${url}/task`, key, { method: "POST", body: JSON.stringify({ prompt: normalizedTask, execution: { sandbox: "docker", network: false, resultLimitBytes: MAX_RESULT_BYTES, diffLimitBytes: MAX_DIFF_BYTES }, budget: options.budget, plan: options.plan, checkpointToken: options.checkpointToken }) }); if (!created.id || created.id.length > 256) throw new Error("coding agent returned an invalid task id"); const deadline = Date.now() + TIMEOUT_MS; while (Date.now() < deadline) { await new Promise((resolve) => setTimeout(resolve, POLL_MS)); const job = await request<CodingAgentJob>(`${url}/task/${encodeURIComponent(created.id)}`, key); if (!["queued", "running", "completed", "failed", "cancelled"].includes(job.status)) throw new Error("coding agent returned an invalid status"); if (job.status === "completed") { const result = normalizeCompletedResult(job.result ?? {}); const structured = parseToolResult({ ok: true, data: result, meta: { provider: "coding-agent", sandbox: "docker" } }); if (!structured) throw new Error("coding agent returned an invalid structured result"); return { output: JSON.stringify(result), warnings: typeof result.review === "string" ? result.review : "", diff: result.diff || null, artifacts: result.artifacts || null }; } if (job.status === "failed") throw new Error(typeof job.error === "string" && job.error.length <= 4_000 ? job.error : "coding agent failed"); if (job.status === "cancelled") throw new Error("coding agent task was cancelled"); } await requestCancel(url, key, created.id); throw new Error("coding agent timed out after 5 minutes; cancellation was requested"); }
