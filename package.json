{
  "name": "bobai",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace=web",
    "api": "npm run dev --workspace=@bobai/api",
    "ai": "tsx scripts/start.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  }
}