# BobAI handoff — 2026-09-11

This pass completed the remaining repository-side cleanup around account deletion, research quota correctness, and scheduled research. Account purge now removes the newly added research and agent artifact records and safely handles shared workspaces by transferring ownership to another member before removing the deleted owner. Research quotas now use an atomic PostgreSQL upsert so concurrent requests cannot bypass the hourly request/source limits.

Scheduled research briefs are durable, authenticated, bounded by a minimum one-hour interval and maximum thirty-day interval, quota-aware, and executed by a background worker. Research claims retain explicit source mappings and confidence, with contradiction signals for related claims that differ in polarity or numeric values. Agent runs have dependency validation, budgets, checkpoints, confidence escalation, cancellation, persistent step artifacts, and bounded results. Capability execution has durable idempotency and global JSON result-size enforcement. OAuth uses authorization-code + PKCE with encrypted verifier state and encrypted stored credentials.

The code-side implementation is now intentionally not claiming live infrastructure. The remaining checklist is limited to deployment/runtime verification, credentials and provider capacity, model/provider assets, external object-storage retention proof, frontend-only merge/split UI, and the final build/test/smoke/browser checks. Those are not being fabricated as completed because they require the actual environment or external systems.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only. This handoff must remain paragraph-only and must explicitly state what was added, what is implemented, and what is next. Unverified environment, provider, deployment, hardware, and runtime work must remain blocked rather than being called complete.
