# BobAI handoff — 2026-09-11

The latest repository pass added research-session replay and deletion APIs. A user can replay an authenticated saved research query through the current provider, creating a new session linked to the original session, while session history now exposes stored answers and deletion is ownership-scoped. This completes the repository-side persistence controls for saved research sessions; scheduled briefs and live provider capacity remain separate concerns.

The code-side platform foundations now include workspace roles and invitations, per-file read/write sharing and authorized downloads, workspace-aware tool permission enforcement, editable and mergeable memory with provenance history, hardened account purge, malware scanning before storage, durable idempotency, research/source safety, long-context compaction, coding-agent timeout/diff validation, and a concrete Docker sandbox executor with network isolation, rootless execution, capability dropping, resource limits, bounded workspace/output, and hard timeout.

The remaining repository-side implementation is now concentrated on full agent dependency-graph execution with budget accounting and checkpoint orchestration, stronger research claim-to-source mapping and contradiction analysis, universal executor result-size/idempotency coverage, automation history completion, connector OAuth authorization, sensitive-memory approval policy, and complete automated coverage. The Docker sandbox implementation itself is code-complete; actual Docker availability, image availability, deployment, and end-to-end verification are environment work.

External production credentials, production object storage, production email, live search/media providers, hosting, DNS, model credentials, and hardware/runtime verification are environment work rather than fake code completions. Frontend-specific visual/UI work is intentionally kept separate from this backend/code-side completion pass.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only. This handoff must remain paragraph-only and must explicitly state what was added, what is implemented, and what is next. Unverified environment, provider, deployment, hardware, and runtime work must remain blocked rather than being called complete.
