# BobAI handoff — 2026-09-11

This repository pass completed a real agent orchestration layer around the durable task queue. Agent plans now support validated dependency graphs with cycle and missing-dependency rejection, bounded step/token/time budgets, explicit human checkpoints, cancellation, queue wake-up after approval, and persistent orchestration state and artifacts through the existing task record. The coding-agent bridge now receives a sandbox execution contract and rejects production results that are not explicitly sandbox-verified, while execution results, diffs, reviews, and artifacts are bounded by a central execution guard.

Research now has durable claim-to-source mappings with source-index validation and confidence metadata, plus a durable per-user hourly request/source quota. Saved research replay consumes the same quota, and the history API exposes claims. Generic OAuth 2.0 authorization-code + PKCE support is now repository-complete: provider configuration is HTTPS-only, state is one-time and ownership-bound, PKCE verifiers are encrypted at rest, callback token exchange is bounded, and returned access/refresh credentials are encrypted before being stored as an integration.

The previously implemented security and reliability foundations remain in place, including authenticated workspace collaboration, file sharing and authorization, malware scanning before storage, account export and purge, memory provenance and merge/history controls, durable idempotency, research/source safety, MCP registry and SSRF hardening, coding-agent timeout and diff validation, and the concrete network-disabled Docker sandbox executor. The new orchestration, research, and OAuth code has been added without claiming that external providers or Docker have been verified.

The remaining repository-side work is now narrowed to sensitive-memory approval semantics, richer research contradiction and saved-brief workflows, automatic media capability selection and the remaining media pipeline contracts, confidence-based agent escalation and durable artifact storage beyond task JSON, universal executor guard and idempotency coverage, automation history completion, and production security/operations policy code where it can be implemented without pretending to have infrastructure. External credentials, Docker runtime deployment, object storage, live providers, model weights, hosting, DNS, and all build/test/runtime verification remain environment work.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only. This handoff must remain paragraph-only and must explicitly state what was added, what is implemented, and what is next. Unverified environment, provider, deployment, hardware, and runtime work must remain blocked rather than being called complete.
