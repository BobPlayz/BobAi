# BobAI handoff — 2026-09-11

The latest repository pass added a real code-side disposable coding sandbox executor contract. It launches coding work inside a rootless Docker container with no network, read-only container root, dropped Linux capabilities, no-new-privileges, PID limits, CPU and memory limits, a bounded writable workspace, bounded output, strict file-path validation, and a hard timeout. The sandbox runtime is selected explicitly through configuration and fails closed when unavailable. This is now a concrete isolated executor implementation rather than only an attestation flag; actual deployment and end-to-end verification still require the runtime environment.

The repository also now has a sandbox policy unit test covering network fail-closed behavior and resource clamping. Existing code-side work includes workspace roles and invitations, per-file read/write sharing and authorized downloads, workspace-aware tool permission enforcement, editable and mergeable memory with provenance history, hardened account purge, malware scanning before storage, durable idempotency, research/source safety, long-context compaction, and coding-agent timeout/diff validation.

The remaining repository-side implementation is now narrowed to the parts that are genuinely not yet complete: full agent dependency-graph execution with budget accounting and checkpoint orchestration, stronger research claim/replay collections, universal executor result-size enforcement and remaining idempotency coverage, automation history completion, connector OAuth authorization, sensitive-memory approval policy, and complete automated coverage. The Docker sandbox itself is implemented in code, while deployment and end-to-end runtime verification remain environment work.

External production credentials, production object storage, production email, live search/media providers, hosting, DNS, model credentials, and hardware/runtime verification are environment work rather than fake code completions. Frontend-specific visual/UI work is intentionally kept separate from this backend/code-side completion pass.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only. This handoff must remain paragraph-only and must explicitly state what was added, what is implemented, and what is next. Unverified environment, provider, deployment, hardware, and runtime work must remain blocked rather than being called complete.
