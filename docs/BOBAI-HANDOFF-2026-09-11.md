# BobAI handoff — 2026-09-12

The latest supplied local validation is now clean at the repository test and build level: the security suite reports 11 of 11 passing tests, the API and web production builds complete successfully, and Drizzle reports no schema changes. The maintenance cleanup and automation worker startup still attempt the intentionally unreachable test database at 127.0.0.1:1, so those database-backed paths remain environment-dependent rather than failed code tests.

The final repository audit found two consistency issues and patched them. The root environment template was stale compared with the API environment contract, including obsolete Ollama settings and missing production configuration fields, so it has been aligned with the current provider-based configuration while retaining the web settings. The development launcher also still tried to start Ollama even though the current model-provider architecture uses BOBAI_MODEL_URL and explicitly does not require Ollama, so the launcher is now provider-agnostic and starts only the API and web processes. Production MFA configuration validation was also tightened to verify that BOBAI_MFA_ENCRYPTION_KEY actually decodes to exactly 32 bytes, matching the AES-256-GCM implementation instead of merely checking string length.

The remaining unchecked work is genuinely environment or external-system dependent: production environment variables and provider credentials, object storage/email/search/media configuration, hosting/DNS, model weights, Docker runtime deployment, retention and backup proof, security monitoring, external load or penetration verification, provider connectivity, browser verification, sandbox end-to-end execution, and a real API smoke run against a running service. Conversation merge/split and multi-device UI remain frontend-owned work and are outside this backend pass.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only and this handoff must remain paragraph-only. Unverified build, test, provider, deployment, runtime, hardware, and environment work must never be represented as completed merely because the repository contains the corresponding implementation.
