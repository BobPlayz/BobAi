# BobAI handoff — 2026-09-11

This pass closed two integration gaps found during the final audit. The collaboration router is now actually registered in the API, and account purge now removes the newly introduced research and agent records while preserving shared workspaces by transferring ownership to another member before deleting the former owner. Research quota accounting is atomic and scheduled research brief claiming is protected against duplicate execution across multiple API instances.

Scheduled research briefs are durable, authenticated, quota-aware, bounded to safe intervals, and executed by a background worker. Research claims have explicit source mappings, confidence, and contradiction signals. Agent runs have validated dependency graphs, bounded budgets, confidence escalation, human checkpoints, cancellation, and durable step artifacts. The API has global JSON response bounds, capability idempotency, sensitive-memory approval, media safety, MCP SSRF protections, file malware scanning, durable collaboration, account export/purge, and the Docker sandbox contract with production attestation gating. Production OAuth uses authorization-code + PKCE with encrypted verifier and integration credentials.

The repository-side implementation is now considered feature-complete for the backend scope covered by this pass. The remaining items are verification and external-system work: install/build/test execution, API/browser smoke tests, real provider credentials and capacity, production object storage/email/search/media infrastructure, Docker runtime deployment, model weights/provider deployment, hosting/DNS, and proof of retention/backup behavior in the actual environment. Frontend-only merge/split and multi-device UI remains outside this backend pass.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only. This handoff must remain paragraph-only and must explicitly state what was added, what is implemented, and what is next. Unverified environment, provider, deployment, hardware, and runtime work must remain blocked rather than being called complete.
