# BobAI handoff — 2026-09-12

The latest supplied local validation is clean at the repository test and build level: the security suite reports 11 of 11 passing tests, the API and web production builds complete successfully, and Drizzle reports no schema changes. The development stack was then started against the user's actual configured database after the migration runner fix. The repository migrations were successfully caught up through 0025, including action_idempotency, rate-limit, agent/OAuth/artifact, and research_briefs schema changes. Startup then exposed a real postgres-js parameter-binding defect in idempotency maintenance cleanup: raw Drizzle SQL interpolation passed JavaScript Date objects directly into postgres-js, which rejects them for bound parameters. That defect was patched by converting the timestamp predicates to ISO timestamp strings.

The next runtime start exposed the same postgres-js Date-binding compatibility problem in research-quota maintenance cleanup. The cleanup query passed a JavaScript Date directly through raw Drizzle SQL, producing ERR_INVALID_ARG_TYPE while the API itself continued listening. This code-side defect has now been patched by converting the research-quota cutoff to an ISO timestamp string before parameterized SQL interpolation. The migration schema is already present, so this failure is not another missing-migration problem.

The latest browser/runtime check reached the real registration path successfully and confirmed that the database and schema are healthy through the readiness endpoint. The registration attempt exposed a code-side error-classification defect: PostgreSQL reported a duplicate email with SQLSTATE 23505 inside DrizzleQueryError.cause, while the route only checked the top-level error code. That caused a normal existing-account conflict to be incorrectly returned as HTTP 503 with the misleading message “account service unavailable.” The auth service now recognizes nested PostgreSQL unique-violation errors and converts them to an explicit ACCOUNT_EXISTS domain error, while the registration route returns HTTP 409 with the generic message “email or username is already in use.” No password, token, or database credential is exposed by that response.

The current auth/UI pass adds a purpose-scoped email OTP sign-in path for users who no longer remember their password. Login OTPs use a separate purpose value from email-verification OTPs, requests remain non-enumerating, successful OTP sign-in still honors MFA, and the existing secure session-cookie flow is reused. This is code-complete but still requires a real email delivery/runtime check before it can be marked operationally verified.

Password policy is now 6 to 128 characters, and the previous external breached-password lookup has been removed from both server enforcement and the signup checklist. The account creation UI now shows only the useful password length guidance instead of the visually noisy breach-check status.

Social sign-in now has server-side Google, Apple, and GitHub authorization-code flows with PKCE, one-time state records, encrypted code-verifier storage, Apple nonce verification, provider identity records, verified-provider-email checks, and existing-account linking. Clicking a provider button therefore starts the real provider OAuth flow, allowing the provider's existing browser session to authenticate the user without BobAI faking a social login. Provider credentials and callback registration remain environment-dependent and therefore are not marked verified.

The public localhost root has also been separated from the authenticated chat workspace. The root page is now a clean BobAI landing page with a single focused entry point, while the existing chat experience remains at /chat. This removes the previous public chat overlays and prevents unauthenticated navigation from immediately generating repeated protected chat/project/conversation requests. The auth experience has now been consolidated into the login page itself: sign-in, signup, passwordless email-code recovery, email verification, and MFA are modes inside one compact card rather than separate auth screens. Google, Apple, and GitHub are represented by small icon-only provider buttons instead of three large full-width buttons, matching the requested compact reference style. The /signup and /verify-otp routes now redirect into that same unified auth surface, and the landing page's create-account action opens the signup mode directly.

The remaining unchecked work is genuinely environment or external-system dependent: production environment variables and provider credentials, object storage/email/search/media configuration, hosting/DNS, model weights, Docker runtime deployment, retention and backup proof, security monitoring, external load or penetration verification, provider connectivity, browser verification, social-provider callback verification, real email OTP delivery verification, sandbox end-to-end execution, and a real API smoke run against a running service. The repository changes in this pass have not been represented as browser/build/provider verified merely because the implementation exists.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only and this handoff must remain paragraph-only. Unverified build, test, provider, deployment, runtime, hardware, and environment work must never be represented as completed merely because the repository contains the corresponding implementation.
