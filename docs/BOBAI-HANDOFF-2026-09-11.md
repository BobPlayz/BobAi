# BobAI handoff — 2026-09-11

This pass completed explicit sensitive-memory approval semantics. Sensitive memory content is still rejected by default, but an authenticated user can issue a short-lived, one-time approval bound to the workspace and memory key, and the approval is consumed and audited before sensitive content is retained or changed. This preserves the safe default while providing a deliberate opt-in path instead of silently storing sensitive information.

Agent orchestration now has validated dependency graphs, bounded step/token/time budgets, checkpoints, cancellation, queue wake-up after approval, and persistent orchestration metadata. Research has durable claim-to-source records, confidence metadata, and per-user hourly request/source quotas. OAuth has generic authorization-code + PKCE state handling, encrypted PKCE verifier storage, HTTPS-only provider validation, bounded token exchange, and encrypted integration credential persistence.

The repository also contains the concrete Docker sandbox executor and coding-agent sandbox-attestation gate, durable idempotency, workspace collaboration, authenticated file sharing, malware scanning before storage, account export/purge, memory provenance/history/merge, research source safety, MCP registry and SSRF hardening, and bounded coding-agent results. These are code implementations only; no external runtime, provider, Docker, hosting, or hardware verification is being represented as complete.

The remaining code-side pass is now focused on deeper research contradiction/saved-brief behavior, confidence-based agent escalation and durable artifact storage beyond task JSON, universal executor result and idempotency coverage with destructive-action audit semantics, the remaining media pipeline contracts, and production-oriented security/operations automation that can be safely implemented in-repository. After those repository changes are finished, the remaining work should be limited to build/tests and environment-dependent credentials, infrastructure, runtime verification, and model/provider deployment.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only. This handoff must remain paragraph-only and must explicitly state what was added, what is implemented, and what is next. Unverified environment, provider, deployment, hardware, and runtime work must remain blocked rather than being called complete.
