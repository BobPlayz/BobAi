# BobAI handoff — 2026-09-12

The latest supplied local validation is clean at the repository test and build level: the security suite reports 11 of 11 passing tests, the API and web production builds complete successfully, and Drizzle reports no schema changes. The maintenance cleanup and automation worker startup initially showed expected database-dependent errors against the local test database at 127.0.0.1:1 during tests. When the development stack was started against the user's actual configured database, additional schema-state errors appeared: action_idempotency and research_briefs were missing and users.deleted_at was absent. These are real environment migration-state problems, not test-suite failures.

The migration audit found a real code-side defect behind that state: scripts/migrate.ts filtered migration files with a pattern that rejected the repository's zero-padded filenames such as 0001_*.sql, so the migration runner could silently skip the entire migration directory. The runner now discovers zero-padded migrations and can recognize an already-existing initial schema baseline before recording it, then proceeds through the remaining migrations. This preserves checksum tracking and the existing advisory migration lock while allowing an existing database to catch up instead of trying to recreate its baseline tables.

The final repository audit also patched the stale root environment template, removed the development launcher's unnecessary Ollama dependency, and tightened production MFA encryption-key validation to require exactly 32 decoded bytes. The current model-provider architecture is provider-agnostic and uses BOBAI_MODEL_URL, BOBAI_MODEL_KEY, and BOBAI_MODEL_NAME rather than requiring Ollama.

The remaining unchecked work is genuinely environment or external-system dependent: applying and verifying the repository migrations against the real configured database, production environment variables and provider credentials, object storage/email/search/media configuration, hosting/DNS, model weights, Docker runtime deployment, retention and backup proof, security monitoring, external load or penetration verification, provider connectivity, browser verification, sandbox end-to-end execution, and a real API smoke run against a running service. Conversation merge/split and multi-device UI remain frontend-owned work and are outside this backend pass.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only and this handoff must remain paragraph-only. Unverified build, test, provider, deployment, runtime, hardware, and environment work must never be represented as completed merely because the repository contains the corresponding implementation.
