# BobAI handoff — 2026-09-11

This pass extended agent runs with confidence thresholds and escalation, durable cross-step artifacts, and an authenticated artifact history endpoint. Each completed step can persist its result and confidence separately from the task JSON, while low-confidence results create an explicit human checkpoint instead of silently continuing. The global API response middleware now bounds JSON responses to two megabytes, and provider capability execution is covered by the same bound. Capability jobs and direct capability executions now participate in the database-backed idempotency layer.

Sensitive-memory retention is explicitly opt-in through a short-lived, one-time approval bound to the workspace and memory key. Research has durable claim-to-source mappings, confidence metadata, replay support, and per-user hourly request/source quotas. OAuth authorization-code + PKCE is implemented with encrypted verifier storage, one-time ownership-bound state, HTTPS-only endpoints, bounded token exchange, and encrypted integration credentials.

Media generation now applies a repository-side safety policy before provider execution, including stronger protections around sexual content involving minors and graphic violence. Existing automatic agent skill inference, provider abstractions, authenticated collaboration, file malware scanning, account export/purge, memory history/merge, MCP SSRF defenses, coding-agent sandbox attestation, and Docker sandbox policy remain implemented.

The remaining repository-side work is now narrowed to richer research contradiction and saved/scheduled brief workflows, media retention and deeper OCR/layout contracts, production security/operations automation that does not require live infrastructure, and final audits for any executable route that still lacks durable idempotency. External credentials, provider capacity, Docker runtime deployment, object storage, model weights, hosting, DNS, and all build/test/runtime verification remain environment work.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only. This handoff must remain paragraph-only and must explicitly state what was added, what is implemented, and what is next. Unverified environment, provider, deployment, hardware, and runtime work must remain blocked rather than being called complete.
