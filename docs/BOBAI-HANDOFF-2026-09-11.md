BobAI remains a single user-facing Bob model with a central tool policy boundary. Repository work covers bounded tool execution, approvals, validation, cancellation, network-target checks, provider limits, bundle integrity, audio limits, webhook limits, result limits, agent-plan bounds, complete checked-in API test execution, production OAuth redirect validation, bounded scanner and backup output, bounded BobHS durable queue state and job payloads/results, stronger SSRF rejection, dependency-lock synchronization, immutable GitHub Actions pinning, pinned Lighthouse tooling, public web SEO/discovery metadata, sitemap and robots coverage, a web manifest, machine-readable llms.txt, accessible public navigation, FAQ structured data, distributed rate limiting, production readiness privacy, consistent public-site documentation, corrected native model lifecycle behavior, safe specialist capability narrowing, corrected provider model-status naming, retirement of obsolete coding orchestration, local-media test alignment, dependency-isolated orchestration tests, mapped IPv6 SSRF coverage, WAV duration coverage, environment-independent backend security tests, corrected BobHS state-loader syntax, safe undefined tool-result serialization, typed BobHS request normalization, asynchronous model-lifecycle state narrowing, a single-main-branch repository policy with Dependabot proposals disabled, corrected agent-plan duration-budget classification, type-safe BobHS deployment volume normalization, corrected FAQ JSX class attributes that blocked the web production build, an idempotent dependency-lock metadata synchronization workflow that avoids duplicate roadmap and handoff entries, a valid JavaScript repository preflight script, aligned CodeQL action pins, a model-training CI workflow that installs its declared Python dependencies before importing torch-backed tests, a repository-owned final training corpus builder with multilingual, conversation, evaluation, coding, agent/tool, security, recovery, verification, memory, API, sandbox, anti-sycophancy, emotional-attunement, epistemic/source-checking, computer-use, MS Paint, Blender/3D, and media-routing curriculum, a validated multimodal asset-manifest pipeline for image/audio/video/3D/music/document/table/screenshot data, and a rights-gated teacher-distillation builder for user-authorized OpenAI-compatible teacher models.

The main runtime now exposes first-class computer, paint, and blender tools. They use the existing permission/approval/audit/tool-loop architecture and execute through BOBAI_COMPUTER_PROVIDER_URL with an optional BOBAI_COMPUTER_PROVIDER_KEY. Blender supports create_model, edit_model, render, export, and verify intents, while Paint supports drawing/editing plus requested artifact saving. The central tool router is instructed to use research for current, changing, niche, disputed, or externally verifiable claims and to use execution tools when the user asks BobAI to actually operate software or create artifacts. The main chat system prompt explicitly rejects reflexive agreement and empty praise, requires calibrated uncertainty and source-backed verification when appropriate, reacts to user emotion without pretending to have human feelings, treats external content as untrusted, and requires result verification before claiming completion.

The production training map is documented in model-training/PRODUCTION_TRAINING.md and model-training/training_capabilities.json. build_final_dataset.py now merges the public multilingual sources, original BobAI capability curriculum, and any explicitly eligible teacher JSONL under model-training/data/teacher. build_teacher_dataset.py can generate synthetic examples from a user-configured OpenAI-compatible teacher endpoint only when --confirm-rights is passed. build_multimodal_manifest.py validates mounted licensed multimodal assets, source/license metadata, expected extensions, file hashes, and deduplication. Actual binary media assets, teacher outputs, real desktop/computer providers, production compute, final training, evaluation, native Windows media compilation, artifact verification, and production-scale deployment remain environment/resource work rather than missing repository architecture.

Future changes must inspect main first and update roadmap.md and this handoff together. Related files may be changed in one coherent commit. Actionable repo-side fixes should be implemented immediately when they can be completed safely. Repository-side implementation should continue until no safe code or documentation work remains that can be completed without developer machine access, credentials, deployment, hardware, licensed external datasets/models, or a real-world decision.
