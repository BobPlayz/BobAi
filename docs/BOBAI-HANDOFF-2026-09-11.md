# BobAI handoff — 2026-09-11

The latest repository pass added complete editable-memory lifecycle primitives alongside the collaboration and executor security work. Memories can now be merged through an authenticated workspace-scoped endpoint. The merge keeps a bounded combined record, preserves the stronger importance while averaging confidence, records before/after history, links the source record in provenance metadata, archives/deletes the source record, and refreshes the target embedding. A bounded history endpoint exposes the recorded lifecycle snapshots.

Workspace collaboration is implemented at the API and persistence layer. Workspace members can be listed and managed under a role hierarchy, administrators can create and revoke expiring invitation tokens, invitees must authenticate with the invited email address before acceptance, and membership changes are auditable. File sharing is persisted separately with per-user read/write permission records, requires workspace membership, supports listing and revocation, exposes shared-file discovery, and provides an authenticated shared-download path.

The tool executor evaluates the authenticated workspace member's role and explicit permission grants before an executable tool can become ready. Owners and admins have full tool permission, viewers and members receive read access by default, and non-read permissions require explicit grants unless the member is privileged. Approval tokens remain bound to user, workspace, and tool and are atomically consumed.

Account deletion is hardened to purge user-owned database records and dependent records, including shares, invitations, tool state, idempotency state, rate-limit state, memories, files, sessions, personal workspaces, and the user account. Local file objects referenced by deleted uploads are removed before the database transaction. External object-storage lifecycle verification remains environment-dependent because the repository's current object-storage implementation is local filesystem storage.

The file upload path scans uploads before object storage, production scanning fails closed when the scanner is unavailable, scanner responses are bounded and schema-checked, and processing status reports indexing and OCR as queued or pending instead of falsely claiming completion. Stored-file downloads are bounded by the application file-size limit.

The action-idempotency layer is durable and bounded. Completed responses can be replayed, a key cannot be reused for a different request payload, abandoned processing claims expire, oversized responses are not persisted, and maintenance removes expired or abandoned records. Image generation, automation execution, and MCP execution use it. MCP argument-size validation happens before consuming the one-time approval token.

The coding-agent boundary requests cancellation after a hard timeout and validates optional returned diffs for bounded size and basic patch structure before accepting the structured result. This is validation and cancellation signaling, not a claim of genuine filesystem/process isolation. Production still requires a real sandbox-attestation mechanism and executor enforcement.

The remaining repository-side implementation is concentrated on richer sensitive-memory approval policy, research claim/replay collections, complete agent dependency/budget/checkpoint execution, universal executor result limits and remaining idempotency coverage, automation history, connector OAuth authorization, and a genuine isolated coding runtime. External provider credentials, production object storage, production email, deployment infrastructure, model credentials, and hardware/runtime verification are environment work rather than fake code completions.

Before every future commit, roadmap.md and this handoff must both be updated. roadmap.md must remain checklist-only. This handoff must remain paragraph-only and must explicitly state what was added, what is implemented, and what is next. Unverified environment, provider, deployment, hardware, and runtime work must remain blocked rather than being called complete.
