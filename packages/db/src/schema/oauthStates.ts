import { index, pgTable, text, timestamp, uuid } from "drizzle-orm/pg-core";
export const oauthStates = pgTable("oauth_states", { id: uuid("id").defaultRandom().primaryKey(), workspaceId: uuid("workspace_id").notNull(), userId: uuid("user_id").notNull(), provider: text("provider").notNull(), stateHash: text("state_hash").notNull().unique(), codeVerifierHash: text("code_verifier_hash"), redirectUri: text("redirect_uri").notNull(), expiresAt: timestamp("expires_at").notNull(), consumedAt: timestamp("consumed_at"), createdAt: timestamp("created_at").defaultNow().notNull() }, (table) => ({ expiryIdx: index("oauth_states_expiry_idx").on(table.expiresAt), userIdx: index("oauth_states_user_idx").on(table.userId) }));
