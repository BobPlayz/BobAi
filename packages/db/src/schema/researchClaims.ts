import { index, integer, pgTable, text, timestamp, uuid, jsonb } from "drizzle-orm/pg-core";
export const researchClaims = pgTable("research_claims", { id: uuid("id").defaultRandom().primaryKey(), sessionId: uuid("session_id").notNull(), userId: uuid("user_id").notNull(), claim: text("claim").notNull(), sourceIndexes: jsonb("source_indexes").notNull(), confidence: integer("confidence").notNull().default(0), contradiction: text("contradiction"), createdAt: timestamp("created_at").defaultNow().notNull() }, (table) => ({ sessionIdx: index("research_claims_session_idx").on(table.sessionId), userIdx: index("research_claims_user_idx").on(table.userId) }));
