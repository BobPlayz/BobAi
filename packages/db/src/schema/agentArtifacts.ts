import { index, jsonb, pgTable, text, timestamp, uuid } from "drizzle-orm/pg-core";
export const agentArtifacts = pgTable("agent_artifacts", { id: uuid("id").defaultRandom().primaryKey(), taskId: uuid("task_id").notNull(), workspaceId: uuid("workspace_id").notNull(), createdBy: uuid("created_by"), stepId: text("step_id").notNull(), kind: text("kind").notNull(), content: jsonb("content").notNull(), createdAt: timestamp("created_at").defaultNow().notNull() }, (table) => ({ taskIdx: index("agent_artifacts_task_idx").on(table.taskId, table.createdAt), workspaceIdx: index("agent_artifacts_workspace_idx").on(table.workspaceId, table.createdAt) }));
