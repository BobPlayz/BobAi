import { index, integer, jsonb, pgTable, text, timestamp, uuid, boolean } from "drizzle-orm/pg-core";
export const researchBriefs = pgTable("research_briefs", { id: uuid("id").defaultRandom().primaryKey(), workspaceId: uuid("workspace_id").notNull(), userId: uuid("user_id").notNull(), name: text("name").notNull(), query: text("query").notNull(), options: jsonb("options"), intervalMs: integer("interval_ms").notNull(), enabled: boolean("enabled").notNull().default(true), lastRunAt: timestamp("last_run_at"), nextRunAt: timestamp("next_run_at"), lastSessionId: uuid("last_session_id"), createdAt: timestamp("created_at").defaultNow().notNull(), updatedAt: timestamp("updated_at").defaultNow().notNull() }, (table) => ({ userIdx: index("research_briefs_user_idx").on(table.userId, table.createdAt), scheduleIdx: index("research_briefs_schedule_idx").on(table.enabled, table.nextRunAt) }));
